name: Test

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: |
          docker build . --file Dockerfile --tag test-image --target run-release
          docker run --rm --entrypoint /usr/bin/echo test-image add test command
